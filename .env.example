# Azure AD app registration (multi-tenant) — see README.md for setup steps
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=http://localhost:3000/api/tenants/consent-callback

# Anthropic API key — used server-side only to generate the AI narrative section
ANTHROPIC_API_KEY=
